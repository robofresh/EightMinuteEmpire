#include <iostream>
#include "Cards.h"

int main()
{
    Deck* deck = new Deck(); //make a deck object
	/* Testing purpose
	Cards* cards = new Cards("0", "invade"); //make a deck object.
	Cards* cards1 = new Cards("1", "invade"); //make a deck object.
	Cards* cards2 = new Cards("2", "invade"); //make a deck object.
	Cards* cards3 = new Cards("3", "invade"); //make a deck object.
	Cards* cards4 = new Cards("4", "invade"); //make a deck object.
	Cards* cards5 = new Cards("5", "invade"); //make a deck object.
	deck->stackofCards->push(cards);
	deck->stackofCards->push(cards1);
	deck->stackofCards->push(cards2);
	deck->stackofCards->push(cards3);
	deck->stackofCards->push(cards4);
	deck->stackofCards->push(cards5);

	deck->initialDraw();
	for (int i = 0; i < deck->drawingcards->faceupcards->size(); i++)
	{
		cout << *deck->drawingcards->faceupcards->at(i)->good << endl;
	}

	deck->drawingcards->exchange(4);

	for (int i = 0; i < deck->drawingcards->faceupcards->size(); i++)
	{
		cout << *deck->drawingcards->faceupcards->at(i)->good << endl;
	}
	*/
}